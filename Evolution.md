# Verified project lessons

- [resolved 2026-07-29] Phase 2 Cangjie Ehall client built and installed only after local stdx dynamic paths were present for both Ohos ABIs; `JsonArray` requires indexed access with `size()` rather than `for-in`. Verification: `BUILD SUCCESSFUL`, phase2 runtime captures. (keywords: stdx JsonArray size BUILD SUCCESSFUL)
- [resolved 2026-07-29] The online x86_64 emulator at `127.0.0.1:5557` installs and foreground-launches the unsigned HAP; capture with `ui_capture.py` plus `hilog_capture.py` produces usable evidence. Verification: phase2-classtable and phase2-score-exam runtime evidence. (keywords: x86_64 emulator ui_capture hilog)
- [resolved 2026-07-29] A real IDS/Ehall session is a hard gate for academic-data verification; slider rejection must remain `BLOCKED_EXTERNAL` and must not be bypassed by persisted or manufactured cookies. Verification: phase2 acceptance records. (keywords: IDS slider Ehall BLOCKED_EXTERNAL)
- [resolved 2026-08-03] A macOS build can fail at `CompileCangjie` when `entry/cjpm.toml` retains Windows `.portage/stdx` paths; point both OHOS ABI `path-option` entries at the installed `/Users/niu/.portage/stdx/1.1.0.1` distributions. Verification: final `BUILD SUCCESSFUL`, x86_64 emulator install and foreground launch. (keywords: can not find path C:/Users stdx CompileCangjie macOS)
- [resolved 2026-08-04] A `100.percent` page placed beside a fixed bottom bar in the same `Column` can squeeze the bar into the system gesture region; wrap conditional page content in a `layoutWeight(1)` container so the bottom bar receives its declared height. Verification: final five-tab screenshots show complete icons/labels and the center toolbox navigation click succeeds. (keywords: bottom navigation gesture bar layoutWeight height squeeze)
- [resolved 2026-08-04] Monochrome SVG media resources can replace placeholder text glyphs and share selected/unselected colors through `Image(@r(app.media.*)).fillColor(...)`; the final campus capture renders 19 image nodes and passes all 5 reference-key assertions. (keywords: ArkUI Cangjie SVG Image fillColor navigation icons)
- [resolved 2026-08-04] Passing a computed Boolean such as `parentState == index` into child `@Prop` initialized correctly but did not refresh the selected bottom-navigation capsule; passing the parent `@State` value directly as `@Prop selectedIndex` and comparing it with the child's immutable index kept selection reactive. Verification: final Pig screenshots show the fourth capsule selected before and after changing the image. (keywords: ArkUI Cangjie State Prop computed expression navigation refresh)
