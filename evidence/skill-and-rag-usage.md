# Skill and RAG usage

| Skill / source | Command or reference | Adopted conclusion |
| --- | --- | --- |
| harmonyos-cangjie-dev | Read complete SKILL.md | Used the required Cangjie incremental migration workflow. |
| harmonyos-project-bootstrap | Read complete SKILL.md | Preserved the existing DevEco project and both ABI targets. |
| cangjie-harmonyos-knowledge | `python .agents/skills/cangjie-harmonyos-knowledge/rag/cjdocs.py --config ./cjdocs.toml doctor` | Failed with `OperationalError: unable to open database file`; raw packaged docs were used as fallback. |
| cangjie-harmonyos-knowledge | `query "ArkUI Button TextInput state onClick"` | Same database failure; no query result was treated as evidence. |
| Packaged ArkUI touch docs | `rag/docs/API/arkui-cj/cj-common-types.md`, `cj-universal-event-touch.md`, `cj-common-events-touch-screen-event.md` | Used TouchEvent coordinates, changedTouches, event type, and nanosecond timestamps to serialize manual slider tracks. |
| Packaged NetworkKit docs | `rag/docs/API/NetworkKit/cj-apis-net-http.md`, `Guide/network/cj-http-request.md` | Used HttpRequestOptions, response code, cookies, and response headers for IDS requests. |
| cangjie-core-reference | Read complete SKILL.md and collection references | Used ArrayList and HashMap with an explicit import to avoid ArkUI Stack namespace ambiguity. |
| harmonyos-build-run-diagnose | `build_analyzer.py`, `ui_capture.py`, `hilog_capture.py` | Built, installed, foreground-checked, captured screenshot/component tree, and bounded target hilog. |
| harmonyos-evolution | Read complete SKILL.md | Recorded only verified Phase 1 conclusions in migration and acceptance records. |

| harmonyos-build-run-diagnose | 2026-08-10 login debug rebuild: `build_recovery.py --retry`, explicit-target `ui_capture.py`, and 8-second `hilog_capture.py` on `127.0.0.1:5555` | Initial compile caught a missing `maskPhone` helper; after repair, HAP built successfully in 25 s 827 ms, installed successfully, launched as PID 11950, and the login page capture contains `loginSubmitButton` and `viewNetworkInteraction`. Evidence: `acceptance/runtime/2026-08-10-login-debug-reinstall/`. |
| harmonyos-cangjie-dev / cangjie-core-reference | Read required workflow and essentials plus existing IDS NetworkKit error paths | Preserved the platform-owned project and added a separator-based raw IDS response channel; login and MFA errors now render the original response body in a bounded scrollable login-page debug panel without persisting credentials or response data. |
| Phase 2 RAG fallback | doctor and query HTTP JSON date time List Scroll state main thread both returned OperationalError: unable to open database file; read packaged NetworkKit, ArkUI list/refresh, state and stdx JSON docs | Used explicit HTTP timeouts/string response type, main-thread launch, Scroll/List rendering and stdx JSON parsing. |
| harmonyos-build-run-diagnose | Phase 2 ssembleApp, ui_capture.py, hilog_capture.py | Timetable, score and exam implementation builds and installs; real academic data remains externally blocked. |
| harmonyos-evolution | Read SKILL.md and verified evidence | Recorded only build/device and external-gate conclusions in phase handoff. |
| cangjie-core-reference | Packaged `std.time` DateTime and comparison reference | Parsed examination start times and implemented deterministic chronological grouping. |
| harmonyos-build-run-diagnose | Hvigor build, x86_64 install/cold launch, `ui_capture.py`, `hilog_capture.py` | Verified the exam slice compiles, packages, launches and produces target-process evidence. |
| Packaged ArkWeb guide | `Guide/web/cj-web-page-loading-with-web-components.md` | Added INTERNET-backed Web component for the seven toolbox destinations. |
| Packaged ArkWeb API | `cj-apis-webview.md` sections `loadUrl`, `canGoBack`, `goBack` | Added guarded URL navigation and Web history return. |
| Cangjie SDK declarations and compile probes | NotificationKit, CalendarKit and FormKit searches plus three Hvigor probes | Marked notification, calendar and desktop card `BLOCKED_EXTERNAL` under SDK 6.1. |
| cangjie-arkts-interop | Read complete SKILL.md plus `cangjie-call-arkts.md`, `hybrid-projects.md`, interop-lib README and packaged `cangjie-load-arkts.md` | Kept the main Ability/UI in Cangjie and isolated unavailable notification/calendar calls behind one JSRuntime bridge. |
| Packaged AccessToken docs | Read complete `cj-apis-ability_access_ctrl.md`, `cj-request-user-authorization.md`, and `cj-permissions-for-all-user.md` | Declared and dynamically requested both calendar user-grant permissions before calling Calendar Kit. |
| Cangjie SDK ArkTS declarations | Read `@ohos.calendarManager.d.ts`, `@ohos.app.form.FormExtensionAbility.d.ts`, `@ohos.app.form.formProvider.d.ts`, and UIAbility `toJSValue` declaration | Implemented the actual calendar callback chain and retained desktop card as an independent ArkTS extension boundary. |
| cangjie-harmonyos-knowledge | `doctor`; queries `ArkUI TextInput AlertDialog checkbox square Cangjie`, `Checkbox init select onChange Cangjie`, and `TextInput input type number keyboard Cangjie`; read `cj-dialog-alertdialog.md#class-alertdialogparamwithbuttons`, `cj-button-picker-toggle.md#示例1-设置开关的样式`, `cj-button-picker-checkbox.md` and `cj-text-input-textinput.md` refs | Confirmed the centered Stack overlay/scrim pattern, `ToggleType.Checkbox` with `size`/`selectedColor`/typed `onChange`, explicit named TextInput construction, and retained stable IDs while matching the supplied SMS verification visual reference. |
| harmonyos-build-run-diagnose | Repeated Hvigor builds, x86_64 install/cold launch, UI tree and bounded hilog capture | IDS fallback, notification/calendar bridge, attendance and public library search produced successful HAPs; latest schoolnet addition awaits build because platform escalation quota was exhausted. |
| Static integrity follow-up | Strict `ConvertFrom-Json` plus targeted UTF-8/mojibake and placeholder scans | Repaired a corrupted calendar permission resource and schoolnet strings before any later build; existing HAP explicitly predates schoolnet. |
| cangjie-harmonyos-knowledge | Query `Cangjie ImageSource createPixelMap readPixelsToBuffer image decode base64 PixelMap`; RAG again returned `OperationalError` | Read packaged image decoding and PixelMap operation guides completely and used `Rgba8888`, `getImageInfo`, `readPixelsToBuffer`, and explicit release. |
| cangjie-core-reference | Complete random, math, and basic data-type references | Ported Flutter random trajectory generation, sigmoid/round operations, numeric conversions, and timed retries. |
| harmonyos-build-run-diagnose | Repeated Hvigor repair loop plus synthetic UI scenarios and bounded hilog | Latest HAP builds, installs and cold-launches; automatic verification remains server-rejected and is not marked PASS. |
| Packaged ArkWeb cookie and script APIs | Read complete `cj-apis-webview.md` sections `WebCookieManager` and `runJavaScript`, plus `cj-web-web.md` Web creation and `onPageEnd` | NetworkKit did not expose IDS Set-Cookie headers; retained the custom Cangjie login UI while submitting the official IDS password form inside a hidden ArkWeb session. Credentials cross only in volatile memory and are cleared after submission; no secret is logged or persisted. |

| cangjie-harmonyos-knowledge | Query `Cangjie NetworkKit HttpData ArrayData bytes response Preferences encrypted token secure storage`; RAG returned `OperationalError: unable to open database file`; read packaged `NetworkKit/cj-apis-net-http.md` and `ArkData/cj-apis-preferences.md` | Used ArrayBuffer captcha responses, bounded HTTP timeouts, Preferences flush/delete, and encrypted-at-rest drinking-water tokens. |
| harmonyos-build-run-diagnose | Dorm-water NetworkKit/Preferences slice: Hvigor dual-ABI build, x86_64 install/launch, `ui_capture.py`, and bounded `hilog_capture.py` | `BUILD SUCCESSFUL in 3 min 1 s 609 ms`; emulator install and PID confirmation passed. Runtime feature interaction remains gated by unavailable IDS home navigation. |
| Cangjie main-thread runtime diagnosis | Read faultlogger `cjerror-io.github.benderblog.traintime_pda.harmonyos-20020065-20260803144535026.log` and moved ArkWeb callback state mutations into `launch {}` | Removed the reproducible login-button process exit caused by updating `@State` outside the UI thread; post-fix cold launch and component-tree capture are clean. |
| harmonyos-build-run-diagnose | Replayed the repository synthetic Login-click scenario after the main-thread fix | The UI reached `Working...`, PID 8697 remained alive, and the target faultlogger gained no post-fix crash; this verifies crash removal only, not genuine authentication. |
| cangjie-core-reference | Read complete `references/cangjie-std/sync/README.md` Timer section | Retained and cancelled `Timer.repeat` across page lifecycle; drinking-water status is polled every 60 seconds and stops after three consecutive idle responses. |
| harmonyos-build-run-diagnose | Rebuilt and reinstalled the timer-complete dorm-water HAP | `BUILD SUCCESSFUL in 1 min 48 s 780 ms`; emulator process PID 11752 and no new target faultlogger entry. |
| harmonyos-cangjie-dev / harmonyos-project-bootstrap | Read complete `SKILL.md` files and inspected `build-profile.json5`, `entry/cjpm.toml`, `module.json5`, and Cangjie sources for the XDYOU launch task | Preserved the existing project, compatible SDK `6.1.0(23)`, and both OHOS ABI targets; no re-scaffolding was performed. |
| cangjie-harmonyos-knowledge | Required `doctor` initially hit the read-only skill directory; built an offline writable index with `--index-dir evidence/cjdocs-index --ai off`, then ran `doctor` and `query "ArkUI Button TextInput state onClick" --top-k 8` | Index health passed with 644 documents and 12,557 sections; vectors=0 is valid for offline lexical retrieval. |
| cangjie-harmonyos-knowledge | Read `docs/API/arkui-cj/cj-text-input-textinput.md#示例代码` and `docs/Guide/arkui-cj/cj-common-components-button.md#场景示例` | Confirmed the existing Cangjie login page's TextInput state/onChange and Button onClick forms before rebuilding. |
| harmonyos-build-run-diagnose | Baseline `build_recovery.py --retry`, first-error analysis, repaired stale stdx paths, rebuilt, then `ui_capture.py` and `hilog_capture.py` against `127.0.0.1:5555` | Final HAP built successfully, installed successfully, foreground PID 30985 was confirmed, UI tree contains `XDYOU`, and bounded target app log has 0 FATAL/ERROR lines. |
| cangjie-harmonyos-knowledge | Reran doctor and ArkUI Preferences queries; local index returned `OperationalError: unable to open database file`; read complete packaged Preferences API and persistence guide | Used Preferences get/put/flush and a dedicated local store without treating RAG snippets as API proof. |
| cangjie-core-reference | Read complete stdx JSON and std.time references plus std.sort comparator examples | Implemented JSON codec, exact-date validation/date arithmetic, and stable occurrence sorting with the global `sort` API. |
| harmonyos-build-run-diagnose | Hvigor dual-ABI repair loop, x86_64 install/launch, three stable-ID UI scenarios, component trees, screenshots, and bounded hilog | CRUD, restart persistence, and synthetic-data cleanup each passed 2/2 assertions; timetable live-data assertion remains open. |
| harmonyos-cangjie-dev | Re-read complete workflow and `cangjie-essentials.md`; reused existing ArkUI State Button conditional rendering and stable IDs | Added the smallest in-memory preview boundary without changing IDS persistence or network code. |
| harmonyos-build-run-diagnose | Dual-ABI Hvigor build plus x86_64 install launch stable-ID entry exit component-tree screenshot and bounded hilog checks | Mock home preview passed 4/4 assertions; exit back to real login passed 3/3; no target fatal line. |
| harmonyos-cangjie-dev / harmonyos-project-bootstrap / harmonyos-evolution | Re-read the complete workflow files, `cangjie-essentials.md`, project configuration, `Evolution.md`, and packaged memory for the five-bottom-tab alignment slice | Preserved the existing DevEco project, compatible SDK `6.1.0(23)`, both OHOS ABI targets, and implemented the UI as a separate incremental Cangjie file. |
| Flutter source UI references | Read `source/lib/page/homepage/home.dart`, `homepage.dart`, Ruisi `home_page.dart`, `toolbox_page.dart`, `pig_page.dart`, `setting.dart`, theme and card sources | Kept the original order and labels: Campus, Ruisi, Toolbox, Pig gallery, Settings; reused the Material 3 blue palette, header/list/card hierarchy, and honest loading/blocked states. |
| cangjie-harmonyos-knowledge | `doctor`; queries `ArkUI Tabs TabContent bottom navigation bar selected index onChange` and `ArkUI Grid GridItem List Divider Toggle Image Cangjie`; read Tabs init/onChange and the returned Grid/Toggle refs from `evidence/cjdocs-index` | Confirmed the offline index is healthy (644 documents, 12,557 sections) and used standard state-driven content switching with explicit stable IDs; no remote synthesis was used. |
| harmonyos-build-run-diagnose | Two successful `build_recovery.py --retry` builds; final install and ten-step `five-bottom-tabs.json` scenario on `127.0.0.1:5555`; final bounded hilog | Final HAP built and installed; all five tabs were clicked and captured, 3/3 assertions passed, PID 30127 was confirmed, and target app lines contain 0 FATAL/ERROR/WARN. |
| harmonyos-cangjie-dev / Flutter source UI reference | Compared `UI截图/0.png` with `source/lib/page/homepage/home.dart`, `homepage.dart`, `homepage_widget_registry.dart`, the Traditional Chinese locale and existing `aligned_home_page.cj` | Rebuilt the campus page and five-item bottom bar around the reference hierarchy, Traditional Chinese labels, beta/schedule/status cards, four-column tools and selected blue navigation capsule while preserving existing navigation callbacks. |
| cangjie-harmonyos-knowledge | Writable-index `doctor`; query `ArkUI Row Column layoutWeight borderRadius backgroundColor bottom navigation selected`; read `layoutWeight`, four-corner `borderRadius`, and `Image.fillColor` refs | Index passed with 644 documents and 12,557 sections. SVG media resources use `Image(@r(...)).fillColor(...)`; weighted page content continues to reserve the fixed bottom bar. |
| harmonyos-build-run-diagnose | Final `build_recovery.py --retry`, install/launch and `home-nav-alignment.json` capture on `127.0.0.1:5555`, then bounded hilog | Final HAP reports `BUILD SUCCESSFUL`, installation and foreground PID 4243 passed, reference-key assertions passed 5/5, and the target remained alive with 0 FATAL; the single UIAbility continuation diagnostic is non-fatal emulator startup noise. |
| harmonyos-cangjie-dev / cangjie-core-reference | Read complete workflow essentials and std time/formatting references for the Phase C campus-card source chain | Preserved controller/model/repository/page structure with main-thread state updates and no fabricated authenticated data. |
| cangjie-harmonyos-knowledge | `doctor` and required ArkUI/NetworkKit queries failed with `OperationalError: unable to open database file`; read raw packaged NetworkKit HTTP and ArkUI TextInput/Image material as fallback | Used explicit response type timeouts status checks redirect Location handling and existing compiled Image data-URI pattern. |
| harmonyos-build-run-diagnose | Repaired stable compiler errors then completed dual-ABI Hvigor build x86_64 install launch 4/4 stable-ID scenario and bounded hilog | Campus-card offline boundary passes; real IDS SSO data remains BLOCKED_EXTERNAL and payment QR was excluded from evidence. |
| harmonyos-cangjie-dev / Flutter and screenshot references | Read `UI截图/6.png`, `source/lib/page/pig/pig_page.dart`, `source/lib/repository/pighub_session.dart`, the Traditional Chinese locale, project configuration, `cangjie-essentials.md`, `Evolution.md`, and packaged memory | Replaced the static Pig placeholder with a source-aligned Traditional Chinese page, real PigHub data chain, loading/error/image states, outlined actions, and the selected Pig navigation capsule. |
| cangjie-harmonyos-knowledge | Writable-index `doctor`; queries for NetworkKit HTTP, network Image events and `@Prop`; read `cj-apis-net-http.md` request/response-type sections, `cj-image-video-image.md` onComplete/onError, `cj-common-types.md` ImageFit, and the complete `cj-macro-prop.md` guide | Used explicit string HTTP responses and timeouts, Image completion/error callbacks, and direct parent `@State` to child `@Prop` synchronization for bottom-navigation selection. |
| cangjie-core-reference | Read complete std random and stdx JSON references | Parsed the real PigHub JSON list, cached it, selected a random item, and avoided immediately repeating the prior PigHub ID. |
| harmonyos-build-run-diagnose / harmonyos-evolution | Final Hvigor build, emulator install, combined before/after interaction scenario, visual inspection, screenshot hashes and bounded hilog | `BUILD SUCCESSFUL in 6 s 985 ms`; 4/4 UI assertions passed; `Change A Pig` changed title and image in one session; target app log contains 0 FATAL/ERROR/WARN. The verified direct-state `@Prop` lesson was recorded. |
| harmonyos-cangjie-dev / Flutter and screenshot references | Read complete workflow and essentials; compared `UI截图/2.png` with `toolbox_page.dart`, `webview_list_tile.dart`, `zh_TW.yaml`, and the existing Cangjie page | Matched the seven-row borderless list, Traditional Chinese copy, spacing, monochrome icons and selected Toolbox navigation state while preserving all source URLs. |
| cangjie-harmonyos-knowledge | Writable-index `doctor`; query `ArkUI Cangjie Image SVG fillColor Row spacing padding list item`; read `cj-image-video-image.md#func-fillcolor-resourcecolor` and `cj-universal-attribute-size.md#func-padding-length-length-length-length` | Index passed with 644 documents and 12,557 sections; used SVG-only `fillColor` tinting and named four-edge padding with explicit vp units. |
| harmonyos-build-run-diagnose | Two successful builds, final emulator install/launch, `toolbox-alignment.json` capture and bounded hilog | Final build passed in 7 s 148 ms; PID 30731 confirmed; all 8 UI assertions passed and target app lines contain 0 FATAL/ERROR/WARN. |
| harmonyos-cangjie-dev / cangjie-core-reference | Read the Flutter sport repository/model/page chain and packaged stdx crypto, encoding and std IO references | Implemented RSA-PKCS1, MD5/hex, Base64/ByteBuffer, real NetworkKit forms, HUKS ciphertext storage and source-structured model/repository/controller/page files. |
| cangjie-harmonyos-knowledge | Required `doctor` and RSA/MD5/form query returned `OperationalError: unable to open database file`; read raw packaged CryptoArchitectureKit MD5, NetworkKit `HttpRequestOptions`, ArkUI TextInput and stdx crypto references | Used the packaged raw docs as the declared fallback; no failed RAG snippet was treated as an API signature. |
| harmonyos-build-run-diagnose | User-side DevEco Studio build and HarmonyOS emulator launch report for the sports working tree | Records only the user-confirmed build/launch boundary; no local PID, screenshot, hilog or genuine sports-data PASS is claimed. |
| cangjie-harmonyos-knowledge | Query `ImageKit createImageSource createPixelMap readPixelsToBuffer NetworkKit HttpData ArrayData` returned `OperationalError`; read complete packaged `cj-image-decoding.md`, `cj-image-pixelmap-operation.md` and ImageKit API sections for byte image sources and pixel buffers | Used RGBA8888 decoding, exact pixel-buffer sizing, explicit PixelMap/ImageSource release and NetworkKit ArrayBuffer image responses. |
| cangjie-core-reference | Read packaged std time, regex, Option and ArrayList sorting references | Used `DateTime.parse`, supported comparisons, grouped regex matches, safe Option rendering and named sort comparator syntax during compiler repair. |
| harmonyos-build-run-diagnose | Rebuilt the interrupted energy slice, repaired first stable Cangjie errors with build analyzer guidance, then ran x86_64 `ui_capture.py` and bounded `hilog_capture.py` | Final HAP build succeeded in 47 s 681 ms; energy boundary scenario passed 3/3 on `127.0.0.1:5557`; target app had 0 FATAL. |
| cangjie-core-reference | Read packaged `std.time` DateTime/Duration reference and reused existing `std.regex`, `std.convert` and stdx JSON patterns | Parsed server timestamps without unverified DateTime constructors, validated IMEI input before requests, and kept response parsing/error paths explicit. |
| harmonyos-cangjie-dev / harmonyos-build-run-diagnose | Re-read both complete workflow files; ran `build_recovery.py --retry`, then `ui_capture.py` and an 8-second `hilog_capture.py` against `127.0.0.1:5555` | Fresh HAP built with `BUILD SUCCESSFUL`, installed successfully, and launched as PID 11260. The foreground tree and screenshot show the XDYOU login page; target-related logs contain 0 FATAL. The sole target-name ERROR is system SCB feature-map noise, not an application-process failure. Evidence: `evidence/redeploy-2026-08-04/`. |
| harmonyos-cangjie-dev / project-bootstrap / evolution | Read all required workflow files plus `cangjie-essentials.md`; inspected project configs, Cangjie sources, `Evolution.md`, packaged memory and the existing settings migration evidence | Preserved the existing DevEco project, compatible SDK `6.1.0(23)`, both OHOS ABI targets and incremental Cangjie-only implementation; recorded only verified visual/runtime conclusions. |
| cangjie-harmonyos-knowledge | Required configured `doctor`/`ArkUI Button TextInput state onClick` query returned `OperationalError`; reran `doctor` and queries with `--index-dir evidence/cjdocs-index --ai off`, then read `cj-button-picker-toggle.md#示例1-设置开关的样式`, `#func-onchange-bool-unit`, `cj-scroll-swipe-scroll.md#scroll`, and four-corner `borderRadius` | Writable offline index passed with 644 documents and 12,557 sections; implemented typed Toggle callbacks, state-backed switches, clipped scrolling and named corner radii from full refs rather than snippets. |
| harmonyos-build-run-diagnose | Unmodified baseline build; first-stable-error analysis; final `build_recovery.py --retry`; Traintime reference HDC captures; final install and `settings-alignment.json`; 8-second target hilog | Final HAP built in 7 s 853 ms, installed and launched as PID 15098; settings scenario passed 9/9 steps and 4/4 assertions; target app lines contain 0 FATAL/ERROR/WARN. Evidence: `evidence/settings-page-alignment-2026-08-04.md`. |
| harmonyos-cangjie-dev / harmonyos-build-run-diagnose | Converted the aligned settings page to the checked-in `zh_TW` copy, rebuilt, reinstalled, replayed the full scroll scenario and captured bounded hilog | Final follow-up build passed in 7 s 842 ms; 9/9 steps and 4/4 assertions passed; target app lines contain 0 FATAL/ERROR/WARN. Evidence: `acceptance/runtime/2026-08-04-settings-traditional-final/`. |
| harmonyos-cangjie-dev / harmonyos-project-bootstrap / harmonyos-evolution | Read complete SKILL files, `cangjie-essentials.md`, project configuration and `Evolution.md` before the Sysj slice | Preserved the existing pure-Cangjie project; this user-scoped slice compiles and runs only the x86_64 emulator target. |
| cangjie-harmonyos-knowledge | `cjdocs.py --config ./cjdocs.toml doctor`; query `NetworkKit HttpRequestOptions redirects response headers Cookie Location`; read `docs/API/NetworkKit/cj-apis-net-http.md#func-request-string-httprequestoptions-asynccallback` and `#class-httprequestoptions` | Confirmed explicit Cookie headers, `HttpRequestOptions` timeouts/string response type and NetworkKit request limits for the Sysj SSO chain. |
| cangjie-core-reference | Read std time, regex, collection and stdx encoding reference indexes | Used `DateTime.parse`, Regex table/cell extraction, `ArrayList` merging/sorting and explicit form/query percent encoding without ArkTS. |
| harmonyos-build-run-diagnose | First build analyzed with `build_recovery.py --retry`; SDK target `6.1.1(24)` missing, then x86_64 rebuild, `ui_capture.py`, and bounded `hilog_capture.py` against `127.0.0.1:5557` | Root target SDK was aligned to installed compatible SDK `6.1.0(23)`; final `BUILD SUCCESSFUL in 1 min 30 s 590 ms`, HAP install/launch PID 16982 and experiment scenario 4/4 passed. Live Sysj data remains externally blocked. |
| harmonyos-build-run-diagnose | Rebuild after page-package refactor, then x86_64 HAP install/launch, five-tab Mock UI scenario, and 8-second target hilog capture on `127.0.0.1:5555` | Final `assembleHap` build passed in 9 s 609 ms. The rebuilt HAP installed and launched as PID 17379. LoginWindow rendered with `mockHomePreviewButton`; the scenario reached and captured all five tabs, with navigation visibility and page-change assertions passing. One historical English `XDYou` text assertion is incompatible with the Traditional Chinese settings page. Target app had no FATAL; its only ERROR was the known non-fatal UIAbility continuation/scene diagnostic. Evidence: `evidence/refactor-layout-runtime/`, `evidence/refactor-layout-navigation-retry/`, and `evidence/refactor-layout-hilog/`. |
| harmonyos-cangjie-dev / harmonyos-project-bootstrap / cangjie-core-reference / harmonyos-evolution | Read complete workflow skills, `cangjie-essentials.md`, project configuration, `Evolution.md`, and packaged memory before migrating the source theme flow | Kept the existing pure-Cangjie project, `6.1.0(23)` SDK configuration, both ABI targets, and no ArkTS boundary. |
| cangjie-harmonyos-knowledge | `cjdocs.py --config ./cjdocs.toml doctor`; query `Environment AppStorage StorageProp ColorMode system dark light` returned an empty packaged index; read raw `Guide/arkui-cj/state_management/cj-environment.md`, `cj-appstorage.md`, and `API/arkui-cj/cj-state-rendering-appstatemanagement.md` | Used the documented pure-Cangjie `Environment → AppStorage → @StorageProp` path. `colorMode` publishes `ColorMode.Light`/`ColorMode.Dark`; app preference uses separate key `xdyouThemePreference`, avoiding an Environment key collision. |
| Flutter source theme migration | Read `source_2.0/lib/repository/preference.dart`, `controller/theme_controller.dart`, `themes/color_seed.dart`, `themes/default.dart`, `themes/green.dart`, `themes/orange.dart`, `page/setting/setting.dart`, and `page/setting/dialogs/change_color_dialog.dart` | Migrated the brightness model (`0 system / 1 light / 2 dark`) semantically as persisted `System`/`Light`/`Dark`, with root-level reactive application. Default palette primary values derive from Flutter `DefaultColor`: light `#3F5F90`, dark `#9FC9FF`; full six-color seed picker remains a separate follow-up migration item. |
| harmonyos-build-run-diagnose | `build_recovery.py --retry`; x86_64 install/launch; `settings-color-mode.json`; 8-second target hilog capture on `127.0.0.1:5555` | Final HAP build reported `BUILD SUCCESSFUL in 11 s 680 ms`; selector taps Dark → Home → Light passed 4/4 assertions, screenshots/component trees were captured under `acceptance/runtime/2026-08-06-settings-color-mode-retry/`, and bounded target hilog was captured. |
| harmonyos-build-run-diagnose | Final page scan, explicit-target HAP install/launch, `ui_capture.py` scenarios, and `hilog_capture.py` | Page sources contain 0 physical lines over 120 characters; the unsigned x86_64 HAP installed and launched as `io.github.benderblog.traintime_pda.harmonyos/EntryAbility` on `127.0.0.1:5555`; 9 of 11 requested scenario runs passed all assertions, while two historical text assertions mismatched current Traditional Chinese/card copy; target app hilog lines contain 0 FATAL, 0 ERROR, and 0 WARN. Detailed evidence: `evidence/page-overlength-refactor-2026-08-06.md`. |
| harmonyos-cangjie-dev / cangjie-core-reference | Read the required workflow and Cangjie essentials; compared the selected attendance page with `HarmonyOS-Examples/07-DeepSeek/entry/src/main/cangjie/src/pages/about_view.cj` | Split long ArkUI builder expressions into named `@Builder` sections and multiline modifier chains while preserving state, callbacks, stable IDs, and behavior. |
| cangjie-harmonyos-knowledge | Required `doctor` and `query "ArkUI Button TextInput state onClick"` command | The configured index returned no ready documents; no empty-index result was treated as API evidence for this formatting-only change. |
| harmonyos-build-run-diagnose | `python .agents/skills/harmonyos-build-run-diagnose/tools/build_recovery.py --retry` | The compact-format follow-up rebuilt successfully after keeping simple expressions on one line and placing every `@State` declaration on one line; HAP `entry-default-unsigned.hap` was generated at 12,958,870 bytes. Explicit-target installation and `EntryAbility` launch on `127.0.0.1:5555` succeeded; UI capture confirmed the target process (PID 2513), and bounded follow-up hilog contains 0 target app-line FATAL/ERROR/WARN. |
| harmonyos-cangjie-dev / required skill family | Read complete workflow, bootstrap, knowledge, core-reference, build-diagnose, evolution skills, `cangjie-essentials.md`, project configs, `Evolution.md`, and packaged memory before the 2026-08-07 login/MFA slice | Kept the existing pure-Cangjie project, SDK `6.1.0(23)`, both ABI targets, incremental structure, main-thread state updates, and stable interactive IDs. |
| cangjie-harmonyos-knowledge | `cjdocs.py --config cjdocs.toml doctor`; queries `ArkUI TextInput password AlertDialog DialogController button onClick` and `Cangjie HTTP POST HttpRequestOptions StringData response callback` | The configured index was empty (`documents: 0`), so no empty result was used as API proof. Raw packaged docs were used as the required fallback. |
| Packaged ArkUI / ArkWeb / NetworkKit refs | Read `API/arkui-cj/cj-text-input-textinput.md`, `cj-dialog-customdialog.md`, `cj-dialog-alertdialog.md`, `cj-web-web.md`, `API/ArkWeb/cj-apis-webview.md` sections `runJavaScript`, `fetchCookie`, `setAcceptCookiesEnabled`, plus existing compiled `HttpRequestOptions` request paths | Implement the non-dismissible second-factor prompt as a state-driven Stack overlay; use a numeric verification-code TextInput, explicit send/confirm/cancel states, official IDS-page JavaScript calls in the existing hidden ArkWeb session, and cookie capture only after IDS leaves the re-auth route. |
| cangjie-core-reference | Read std/stdx routed references and `std.sync` Timer guidance | Use retained/cancelled `Timer.repeat` for resend countdown; keep JSON/protocol parsing inside the official IDS-page JavaScript context so the same browser cookie session continues without persisting password or verification code. |
| harmonyos-build-run-diagnose | Final 2026-08-07 `build_recovery.py --retry`; explicit-target HAP install/launch; `login-validation-2026-08-07.json`; bounded final hilog | Final build reported `BUILD SUCCESSFUL in 6 s 150 ms`; unsigned HAP installed and launched on `127.0.0.1:5555` as PID 327; empty-login validation passed 3/3; bounded capture contains 0 target app-line FATAL/ERROR/WARN. Real password and SMS code remain an explicit credential gate and are not claimed PASS. |
| cangjie-harmonyos-knowledge / cangjie-core-reference | Read ArkWeb `onPageBegin`/`onPageEnd` and universal `zIndex`/visibility docs plus retained `Timer.repeat` guidance while diagnosing the post-password `Working...` stall | The IDS page may remain on the same login URL while a client-side slider waits, so URL-only `onPageEnd` handling is insufficient. Added bounded JavaScript DOM inspection every 2 seconds and expands the same official ArkWeb to full-screen when slider/wait state is detected; the second-factor overlay remains URL/DOM-triggered after slider completion. |
| harmonyos-build-run-diagnose | 2026-08-07 slider watchdog fix build, emulator install, login-shell scenario, and bounded hilog | `BUILD SUCCESSFUL in 6 s 488 ms`; HAP SHA-256 `12fde5a4967ab22679042d783edf57f5a30a9c07a44356a87cb3a4e81cc3629a`; installed/launched on `127.0.0.1:5555` as PID 25397; login validation passed 3/3 and bounded capture had 0 target app-line FATAL/ERROR/WARN. Genuine slider and MFA triggering still require the user's IDS credentials. |
| Dart/Python IDS references | Read `source_2.0/lib/repository/ids_session/ids_session.dart`, `slider_captcha_client.dart`, `ids_reauth_client.dart`, and `python/ids_login_debug.py` complete protocol sections | Replaced the website fallback with a repository-only sequence: login hidden fields → background NCC slider solve (6 rounds × neighboring offsets) → encrypted credentials → redirect classification → NetworkKit SMS re-auth. No slider or official website is rendered. |
| harmonyos-build-run-diagnose | 2026-08-07 slider alignment correction build, emulator restart recovery, explicit-target install/launch, login validation scenario | Final corrected HAP built successfully, installed after emulator restart on `127.0.0.1:5555` as PID 5697, and login validation passed 3/3. HAP SHA-256: `f5e893d12e2ee9036f68c42a2bb515d59a921b2a171eeebc19510220bfbaa32a`. Real slider success still requires the user's credential-gated run. |
| harmonyos-cangjie-dev / cangjie-harmonyos-knowledge / harmonyos-build-run-diagnose | 2026-08-11 diagnosis: read required skill family, project configs, IDS Cangjie/Dart request paths and Evolution stores; configured `doctor` plus queries `ArkUI Button TextInput state onClick` and `NetworkKit HTTP POST redirect Location 405 responseCode HttpRequestOptions` returned `OperationalError: unable to open database file`; read raw `Guide/network/cj-http-request.md` and `API/NetworkKit/cj-apis-net-http.md` `HttpRequestOptions`/`HttpResponse` sections | The login POST shape is valid, but NetworkKit automatically follows redirects and the SDK 23 Cangjie options expose no redirect-disable field. The observed downstream body `Request method 'POST' not supported` is therefore consistent with the credential POST being replayed at the redirected Ehall GET-only route; the callback sees the final 405 and mislabels it as an IDS login failure. This is a diagnosis, not yet a verified fix. |
| harmonyos-cangjie-dev / cangjie-harmonyos-knowledge / harmonyos-build-run-diagnose | 2026-08-11 POST-redirect repair: reused the raw NetworkKit `HttpRequestOptions`, `HttpResponse`, and header-event refs; implemented intermediate Cookie capture, encoded `service`, signature-scoped 405 recovery and an explicit Ehall GET; ran `build_recovery.py --retry`, explicit-target `ui_capture.py`, and bounded `hilog_capture.py` | `SyncCangjieResource` and `assembleHap` passed; the HAP installed on `127.0.0.1:5555`, launched as PID 9639, and the login page plus target-process baseline were captured with zero bundle-name app-line FATAL/ERROR/WARN. Genuine credential replay remains `BLOCKED_EXTERNAL`. Evidence: `evidence/login-post-redirect-405-fix-2026-08-11.md`. |

## 2026-08-11 — real academic data repair

- Skills: `harmonyos-cangjie-dev`, `harmonyos-build-run-diagnose`, `cangjie-harmonyos-knowledge`, `cangjie-core-reference`.
- Source reads: `source_2.0/lib/repository/ehall_session.dart`, score/exam/current-semester/class-table controllers, repositories and pages, plus the real-data screenshot inventory.
- RAG query attempted: `HttpRequestOptions redirect maxLimit followRedirect NetworkKit`; indexed query returned `OperationalError: unable to open database file`.
- Fallback full document read: `.agents/skills/cangjie-harmonyos-knowledge/rag/docs/API/NetworkKit/cj-apis-net-http.md`, `HttpRequestOptions`, response headers/cookies and response-size `maxLimit` sections. Conclusion: `maxLimit` controls response bytes, not redirects; redirects and cookies must be handled from response code, `Location`, headers and cookies.
- Validation: full Hvigor build, overwrite install, ability launch, stable-id component-tree checks and target-process Hilog. Private response fields were neither printed nor persisted.
## 2026-08-11 — campus query live data and preliminary UI alignment

- Skills: `harmonyos-cangjie-dev`, `harmonyos-build-run-diagnose`, `cangjie-harmonyos-knowledge`, and `cangjie-core-reference`; the required workflow files and `cangjie-essentials.md` were read before implementation.
- Source/UI reads: Flutter empty-classroom, attendance and class-table source chains plus `UI真实数据展示截图/空闲教室.png`, `考勤查询.png`, and `日程表.png`.
- Core reference: read the Cangjie `DateTime` and `dayOfWeek` reference before implementing current-week selection and the seven-day timetable layout.
- Build diagnosis: the first timetable macro failure was analyzed with `build_analyzer.py`; the local builder expression was simplified before rebuilding instead of repeating the same command unchanged.
- Runtime verification: final single-emulator-ABI build reported `BUILD SUCCESSFUL in 1 min 21 s 333 ms`; overwrite install, cold launch, stable-id tree checks, screenshots and target Hilog passed on `127.0.0.1:5557`.
- Adopted result: 12 real empty rooms, 34 real timetable arrangements and 57 real Chaoxing attendance rows render without mock data. Attendance semester fallback and UI evidence are documented in `evidence/campus-query-live-data-2026-08-11.md`.

## 2026-08-11 — academic and campus UI alignment follow-up

- Skills: `harmonyos-cangjie-dev`, `cangjie-harmonyos-knowledge`, and `harmonyos-build-run-diagnose`; the workflow files and `cangjie-essentials.md` were read before the change.
- Required commands: `cjdocs.py --config .\cjdocs.toml doctor` and query `ArkUI Button TextInput state onClick`; both returned `OperationalError: unable to open database file`.
- Raw fallback refs read in full: `cj-button-picker-textpicker.md` and `cj-universal-attribute-location.md`. Adopted `TextPicker(range, selected)`, non-looping `onChange`, and absolute `position(x, y)` for the schedule canvas.
- Source/UI reads: score, exam, attendance, empty-classroom and class-table Flutter page/model/controller chains, plus the supplied paired screenshots.
- Build diagnosis: the first build failed because `std.collection.*` and ArkUI both exported `Stack`; `build_analyzer.py` was run, imports were narrowed to `std.collection.ArrayList`, then the build passed instead of repeating the same error.
- Runtime: single-emulator-ABI HAP installed on `127.0.0.1:5557`; stable IDs verified score filters/cards, all exam groups, 57-row attendance, empty-classroom picker controls, and the class-table time grid. Evidence: `evidence/ui-alignment-academic-campus-2026-08-11.md`.

## 2026-08-11 — textual page-header icon correction

- Skills: `harmonyos-cangjie-dev` and `cangjie-harmonyos-knowledge`.
- Query: `ArkUI Button transparent backgroundColor Color Transparent button style`; configured index returned `OperationalError: unable to open database file`.
- Raw refs read in full: `cj-button-picker-button.md` and `cj-universal-attribute-background.md`. The Button default is `Emphasized`; `ButtonStyleMode.Textual` is the documented no-background style and `Color.Transparent` is the documented transparent resource color.
- Adopted: shared back plus score/exam/attendance/empty-classroom/class-table header actions now use textual style and `Color.Transparent` instead of integer `0x00000000`, which rendered as black emphasized circles on the emulator.
- Validation for this focused change is delegated to the user's requested local DevEco build; no build or Git commit was run in this turn.

## 2026-08-11 — energy, library borrow and campus-card live-data wiring

- Skills: `harmonyos-cangjie-dev`, `cangjie-harmonyos-knowledge`, and `harmonyos-build-run-diagnose`; their workflow files and `cangjie-essentials.md` were read before implementation.
- Source/UI reads: complete `source_2.0` energy, library-borrow and campus-card controller/model/repository chains plus paired screenshots under `鸿蒙截图/` and `UI真实数据展示截图/`.
- Required RAG commands: `doctor`, `ArkUI Button TextInput state onClick`, and `NetworkKit HttpRequestOptions redirect maxLimit final URL responseCode cookies`; the configured index returned `OperationalError: unable to open database file`.
- Raw fallback refs read: `API/NetworkKit/cj-apis-net-http.md` (`HttpRequestOptions`, `HttpResponse`, `HeadersReceive`) and `Guide/network/cj-http-request.md`. Adopted intermediate response-header capture because SDK 23 auto-follows redirects and exposes no final URL.
- Implemented: IDS-authenticated energy OAuth/signature/AES/meter history chain; IDS-Chaoxing-OPAC JWT loan-list chain; campus-card redirect Cookie/OpenID recovery and date-range transaction fallback; real summaries on the campus home; source-shaped preliminary detail UIs.
- Validation: direct x86_64 `cjpm build` passed twice after the stable compiler errors were repaired. Full Hvigor packaging could not be rerun because its external cache symlink requires a fresh sandbox escalation; no live-data PASS is claimed until the user builds/runs with the retained real session.

## 2026-08-12 service SSO cookie repair

- Skills: `harmonyos-cangjie-dev`, `cangjie-harmonyos-knowledge`, `cangjie-core-reference`, `harmonyos-build-run-diagnose`.
- RAG commands: `cjdocs.py doctor` and query `ArkWeb WebCookieManager configCookie fetchCookie Cookie SSO redirect` were attempted; the index failed with `OperationalError: unable to open database file`, so the packaged raw ArkWeb reference was read instead.
- Reference: `.agents/skills/cangjie-harmonyos-knowledge/rag/docs/API/ArkWeb/cj-apis-webview.md`, `configCookie(String,String,Bool)`; adopted conclusion: seed each saved IDS cookie before Web component loading and keep service authorization values memory-only.
- Implementation: preserve every cookie pair from NetworkKit/Web cookie text, ignore cookie attributes, pass merged cookies into energy signed requests, and pass ArkWeb payload together with current URL so redirect authorization values are parsed.
- Verification: `assembleApp --no-daemon` completed with `BUILD SUCCESSFUL`; install/start on x86_64 emulator succeeded. Runtime inspection established that the energy service is externally restricted when the HarmonyOS emulator is not on the campus network; live-value acceptance remains `BLOCKED_EXTERNAL` in that network condition.
- Evidence: `acceptance/runtime/2026-08-12-service-auth/ids-cookie-seed/` and `acceptance/runtime/2026-08-12-service-auth/payload-code-final/`. Files contain component trees only; no cookies, tokens, QR payloads, or response bodies were saved.

## 2026-08-12 campus-card AJAX and home summaries

- Skills: `harmonyos-cangjie-dev`, `cangjie-core-reference`, `cangjie-harmonyos-knowledge`, `harmonyos-build-run-diagnose`.
- RAG doctor/query were attempted and returned `OperationalError: unable to open database file`; fallback references read: packaged NetworkKit `cj-apis-net-http.md` and Cangjie stdx JSON README.
- Campus-card transactions now send the source application's AJAX request semantics (`Accept`, `Origin`, `Referer`, `X-Requested-With`) and refuse an HTML authentication page before JSON decoding. No response body is logged or persisted.
- Successful library borrow counts and campus-card balances are cached in process memory only and reused when returning to the campus home. Water/electricity home authentication is no longer started because that service requires campus-network access.
- Verification: `assembleApp --no-daemon` completed with `BUILD SUCCESSFUL`; live account/device validation is intentionally left to the user for this short turn.

## 2026-08-13 — homepage child-page header alignment

- Skills: `harmonyos-cangjie-dev`, `harmonyos-project-bootstrap`, `cangjie-harmonyos-knowledge`, `cangjie-core-reference`, `harmonyos-build-run-diagnose`, and `harmonyos-evolution`; required workflow files and `cangjie-essentials.md` were read before editing.
- Reference images: `UI真实数据展示截图/日程表.png`, `成绩查询.png`, `考试安排.png`, `图书馆信息.png`, `空闲教室.png`, `校园卡流水信息.png`, `考勤查询.png`, and `水电页面_电量信息.png`.
- RAG: `doctor` passed with 644 documents; queries were `ArkUI Button TextInput state onClick`, `ArkUI top app bar navigation back refresh icon button Row Image`, and `ArkUI Image SVG fillColor objectFit onClick component`.
- Refs read in full: `docs/API/arkui-cj/cj-image-video-image.md#func-fillcolor-resourcecolor` and `docs/API/arkui-cj/cj-image-video-image.md#func-objectfit-imagefit`. Adopted SVG `Image` with `fillColor`, `ImageFit.Contain`, transparent textual buttons, and 40vp action targets.
- Implementation: shared SVG back and refresh actions replace text glyphs; title, action size, spacing, color, IDs and behavior are unified across every existing `PageHeader` child page and eleven refresh-capable pages.
- Validation: baseline and final HAP builds both reported `BUILD SUCCESSFUL`; final HAP installed/launched on `127.0.0.1:5555` as PID `22616`; `scenario-subpage-header.json` passed 3/3 and the inspected capture shows both SVG actions. Bounded hilog has 0 FATAL; its one bundle-name ERROR is the system SCB force-split feature-map diagnostic, not the app process.

## 2026-08-13 — library reference UI alignment

- Skills: `harmonyos-cangjie-dev`, `cangjie-harmonyos-knowledge`, `cangjie-core-reference`, `harmonyos-build-run-diagnose`, and `harmonyos-evolution`.
- Source/reference: `UI真实数据展示截图/图书馆信息.png`; Flutter structure from `library_window.dart`, `borrow_list_view.dart`, `borrow_info_card.dart`, and `model/xidian_ids/library.dart`.
- RAG: `doctor` passed with 644 documents; queried `ArkUI Tabs TabContent tab bar indicator divider Cangjie`, `ArkUI Image network URL objectFit placeholder clip borderRadius`, and `Image String URL network image Cangjie Image constructor`.
- Refs read: `docs/API/arkui-cj/cj-navigation-switching-tabs.md#tabs`, `docs/API/arkui-cj/cj-navigation-switching-tabcontent.md#func-tabbar-resourcestr`, and `docs/API/arkui-cj/cj-image-video-image.md#func-objectfit-imagefit`; std time reference supplied `DateTime.parse`/timestamp date-difference behavior.
- Adopted: source-shaped text tabs with a thin selected indicator, real URL cover images with `ImageFit.Cover`, source date columns and computed due-day status, fixed borrow summary, no top-right refresh action, and no homepage bottom navigation while any homepage child page is open.
- Verification: final HAP `BUILD SUCCESSFUL`, installed/launched on `127.0.0.1:5555` as PID `21851`; final library scenario passed 6/6 across borrow and search tabs; bounded hilog has 0 FATAL. Real loan-card pixel verification remains account/session gated and no fake loans were introduced.

## 2026-08-13 — campus-card transaction reference UI alignment

- Skills: `harmonyos-cangjie-dev`, `cangjie-harmonyos-knowledge`, and `harmonyos-build-run-diagnose`.
- Source/reference: `UI真实数据展示截图/校园卡流水信息.png`, `source_2.0/lib/page/schoolcard/school_card_window.dart`, and the existing school-card controller/model/repository/page chain.
- RAG: configured `doctor` and query `ArkUI Cangjie DatePickerDialog date range picker show dialog` both returned `OperationalError: unable to open database file`. The documented raw fallback was used; `docs/API/arkui-cj/cj-button-picker-datepicker.md` was read completely.
- Adopted: SDK 23 Cangjie `DatePicker(selected:)` plus `onDateChange` in a two-step range overlay, current-month defaults, one blue range action, and a white three-column transaction table. Reference-incompatible balance, QR, refresh, editable date inputs, and separate query controls were removed from this detail page.
- Runtime diagnosis: the first date-dialog scenario exposed an uncaught invalid-session exception from `SchoolCardSession.sessionCookie`; exported evidence is `evidence/school-card-date-picker-cjerror.log`. The page now catches that authentication-boundary exception and reports it in UI instead of terminating the Ability.
- Verification: final `BUILD SUCCESSFUL in 10 s 371 ms`; installed/launched on `127.0.0.1:5555` as PID `10634`; date selector open/cancel plus shell assertions passed 7/7. Populated real transaction comparison remains `BLOCKED_EXTERNAL` without a genuine campus-card SSO session.

## 2026-08-13 Mock UI visual correction

- Compared the score, exam, library, and empty-classroom source captures with the supplied HarmonyOS captures; no real-account data was added.
- Skills: `harmonyos-cangjie-dev`, `cangjie-harmonyos-knowledge`, and `harmonyos-build-run-diagnose`.
- RAG query `ArkUI Button Image TextInput Row layout` failed with `OperationalError: unable to open database file`; raw ref `docs/API/arkui-cj/cj-button-picker-button.md#enum-buttonstylemode` was read instead.
- Adopted conclusion: use `ButtonStyleMode.Normal` for explicitly colored filter/search buttons and do not apply `Image.fillColor` to outline SVG icons.

## 2026-08-14 real-data detail UI refinement

- Skills: `harmonyos-cangjie-dev`, `cangjie-harmonyos-knowledge`, `cangjie-core-reference`, `harmonyos-build-run-diagnose`, and `harmonyos-evolution`.
- Compared real-account score, exam, and attendance HarmonyOS captures with their source-app pairs; also compared the empty-classroom controls and calendar reference.
- RAG `doctor` and query `ArkUI Grid calendar dialog TextPicker Button` returned `OperationalError: unable to open database file`; reused the already compiled pure-Cangjie `Grid`/`DateTime` calendar implementation from `school_card_page.cj` and the source Flutter page semantics.
- Implemented shared single-date calendar, source-shaped building list, one-decimal credit/GPA and integer numeric scores, exam time/location/seat icons with compact seating layout, deep-blue `0xFF46699D`, and four-row attendance details including revival count.
- Verification: HAP builds succeeded; the final incremental build reported `BUILD SUCCESSFUL in 1 min 41 s 911 ms`. Replacement install and Ability launch succeeded as PID `31149`. The calendar opened in runtime capture without fatal logs. Ehall subsequently reported an expired session, so populated score/exam/attendance and building-list runtime comparison remains account-session gated.

## 2026-08-13 — campus-card calendar-range dialog alignment

- Skills: `harmonyos-cangjie-dev`, `cangjie-harmonyos-knowledge`, and `harmonyos-build-run-diagnose`.
- Reference: `UI真实数据展示截图/选日期.png`.
- RAG: configured `doctor` and query `ArkUI CalendarPicker month calendar selected date range Cangjie` returned `OperationalError: unable to open database file`; raw fallback refs read completely were `docs/API/arkui-cj/cj-scroll-swipe-grid.md`, `cj-scroll-swipe-griditem.md`, and the Cangjie std time handbook.
- Adopted: a pure-Cangjie fixed 7×6 `Grid` month calendar, `DateTime.dayOfWeek` offset, Gregorian month lengths, previous/next navigation, continuous range background, circular endpoints, dim scrim, and reference `CANCEL`/`OK` actions. No ArkTS interop was introduced.
- Verification: HAP `BUILD SUCCESSFUL in 10 s 505 ms`; installed/launched on `127.0.0.1:5555` as PID `32692`; open, next-month, and cancel scenario passed 8/8; bounded target capture has 0 FATAL and 0 app-line ERROR/WARN.

## 2026-08-13 — campus-card month/year dropdown alignment

- Skills: `harmonyos-cangjie-dev`, `cangjie-harmonyos-knowledge`, and `harmonyos-build-run-diagnose`.
- Reference: `UI真实数据展示截图/下拉选月份.png`.
- RAG: configured `doctor` and query `ArkUI Button child Row triangle dropdown state grid Cangjie` returned `OperationalError: unable to open database file`; raw fallback `docs/API/arkui-cj/cj-button-picker-button.md` was read with the already verified Grid/GridItem refs.
- Adopted: independent child-content `Button` controls for month/year labels and arrow indicators; a state-switched 3×4 month grid and 3×4 year grid; current view uses a blue filled capsule and the draft selection uses a blue outline.
- Verification: HAP `BUILD SUCCESSFUL in 10 s 397 ms`; installed/launched on `127.0.0.1:5555` as PID `18205`; month open, July selection, year open and cancel completed with 3/3 assertions; bounded log contains 0 FATAL and 0 app-line ERROR/WARN.

## 2026-08-13 — campus-card dropdown arrow size follow-up

- Skills: `harmonyos-cangjie-dev` and `harmonyos-build-run-diagnose`.
- Increased the month/year arrow glyph from 9vp to 15vp, added bold weight, an explicit 18×24vp visual box, centered alignment, and slightly wider text spacing while retaining the parent button hit target.
- Verification: HAP `BUILD SUCCESSFUL in 10 s 516 ms`; installed/launched on `127.0.0.1:5555` as PID `26609`; month/year dropdown scenario passed 3/3 and the enlarged arrows were visually inspected in `evidence/school-card-dropdown-arrow-size-2026-08-13/`.

## 2026-08-13 — campus-card dropdown arrow proportion correction

- Skills: `harmonyos-cangjie-dev` and `harmonyos-build-run-diagnose`.
- Matched the supplied screenshot proportion by changing the arrow to 12vp medium weight in a 12×20vp visual box and reducing label spacing from 5vp to 1vp; the parent buttons retain their full interaction targets.
- Verification: HAP `BUILD SUCCESSFUL in 10 s 406 ms`; installed/launched on `127.0.0.1:5555` as PID `31864`; dropdown scenario passed 3/3 and the final month-selector screenshot is under `evidence/school-card-dropdown-arrow-proportion-2026-08-13/`.

## 2026-08-13 — campus-card dropdown arrow 14vp follow-up

- Skills: `harmonyos-cangjie-dev` and `harmonyos-build-run-diagnose`.
- Increased only the arrow glyph and visual width from 12vp to 14vp; retained the 1vp label gap and medium weight so the compact original-image proportion is preserved.
- Verification: HAP `BUILD SUCCESSFUL in 10 s 525 ms`.

## 2026-08-14 — homepage first course-card alignment

- Skills: `harmonyos-cangjie-dev` and `harmonyos-build-run-diagnose`; both workflow files and `cangjie-essentials.md` were read before editing.
- Source/reference: `UI真实数据展示截图/校园信息首页数据展示.png` and `source_2.0/lib/page/homepage/info_widget/classtable_card.dart`.
- Adopted: restored the Flutter card's 32vp calendar icon, 18vp icon-to-copy gap, 14vp subtitle and 16vp empty-state label; preview-only state chips, title, date/week line and empty-state copy now match the supplied reference without manufacturing production schedule data.
- Verification: HAP `BUILD SUCCESSFUL in 8 s 561 ms`; installed/launched on `127.0.0.1:5555` as PID `31201`; `homeScheduleCard` is present at 1208×518 px in the 1320 px-wide emulator capture. The retained real-login session verifies the production card geometry while intentionally preserving its live loading copy.
- Evidence: `evidence/home-course-card-alignment-2026-08-14/`.

## 2026-08-14 — homepage genuine schedule-state calculation

- Skills: `harmonyos-cangjie-dev`, `cangjie-core-reference`, and `harmonyos-build-run-diagnose`.
- Source chain read: `source_2.0/lib/controller/homepage_controller.dart`, `source_2.0/lib/controller/classtable_controller.dart`, `source_2.0/lib/page/homepage/info_widget/classtable_card.dart`, and the Simplified/Traditional Chinese translation keys.
- Cangjie reference read: `references/cangjie-std/REFERENCE.md`, `references/cangjie-std/time/README.md`, and the Timer section of `references/cangjie-std/sync/README.md`.
- Adopted: `DateTime.now()` supplies local time; the card switches to tomorrow after 21:25; term start and semester length determine the 1-based week or `假期中`; day/week filtering plus period-end filtering determines remaining courses; a zero result produces `今日安排完成` and `暂无日程`. A retained `Timer.repeat` refreshes the derived state every minute and is cancelled when the component disappears.
- Verification: HAP `BUILD SUCCESSFUL in 8 s 244 ms`; installed/launched on `127.0.0.1:5555` as PID `7456`; after a 20-second genuine timetable load the component tree contains `今日安排完成`, `8月14日 周五 假期中`, and `暂无日程`.
- Evidence: `evidence/home-course-real-time-2026-08-14/`.

## 2026-08-14 — class-table horizontal week selector

- Skills: `harmonyos-cangjie-dev`, `cangjie-harmonyos-knowledge`, and `harmonyos-build-run-diagnose`.
- Source/reference: `UI真实数据展示截图/日程表.png` and the existing genuine class-table controller/model/page chain.
- RAG: configured `doctor` and query `ArkUI Cangjie horizontal Scroll scrollable ScrollDirection horizontal controller scrollTo` returned `OperationalError: unable to open database file`; raw fallback sections read from `docs/API/arkui-cj/cj-scroll-swipe-scroll.md` were `Scroll.init`, `scrollable`, `Scroller.scrollTo`, and the controller examples.
- Adopted: a full-semester `Row` inside `Scroll(this.weekScroller).scrollable(ScrollDirection.Horizontal)`; fixed-width source-shaped week cells with selected capsule and dot field; `Scroller.scrollTo` centers the current/tapped week. The previous/next buttons and duplicate week-label row were removed.
- Verification: HAP `BUILD SUCCESSFUL in 12 s 124 ms`; installed/launched on `127.0.0.1:5555` as PID `17534`; current-week and post-swipe screenshots were captured; scenario assertions passed 5/5. Bounded hilog has 0 FATAL; its single bundle-name ERROR is the known system SCB force-split feature-map diagnostic.
- Evidence: `evidence/classtable-week-scroll-2026-08-14/` and `evidence/classtable-week-scroll-hilog-2026-08-14/`.

## 2026-08-14 — Class-table real 5×5 week previews

- Skills: `harmonyos-cangjie-dev`, `cangjie-harmonyos-knowledge`, `harmonyos-build-run-diagnose`, and `harmonyos-evolution`; all required workflow files, project configuration, and existing evolution records were read before implementation.
- Source chain: `source_2.0/lib/page/classtable/class_page/week_choice_view.dart`, `class_table_view/class_organized_data.dart`, `current_time_indicator.dart`, and `model/time_list.dart`. Adopted the source meanings: five columns are Monday–Friday, five rows are paired timetable periods plus the first evening block, and opacity `1.0 / 0.45 / 0.25` means active-or-upcoming / completed / vacant.
- RAG: the sandboxed `doctor` initially returned `OperationalError: unable to open database file`; after the user-authorized external execution, `doctor` reported 644 documents and 12,557 sections. Query: `ArkUI Grid GridItem ForEach circle backgroundColor opacity`. Read refs: `docs/API/arkui-cj/cj-graphic-drawing-circle.md#init-length-length` and `docs/API/arkui-cj/cj-graphic-drawing-common.md#func-fillopacity-float64`. Adopted named `Circle(width:, height:)` dimensions and `fillOpacity(Float64)`.
- Implementation: replaced the static 4×4 bullet text with 25 real `Circle` nodes per week; academic courses use week/day/period overlap, custom classes use exact date/minute overlap, and genuine `DateTime.now()` plus the term start determines completed versus upcoming state. A follow-up fixed the service's `yyyy-MM-dd HH:mm:ss` semester-start format by taking its date portion, computes previews directly from the callback payload instead of newly assigned `@State`, and uses pre-mixed solid RGB colors because `Shape.fillOpacity` did not preserve the intended differences on the emulator.
- Validation: the final follow-up `build_recovery.py --retry` reported `BUILD SUCCESSFUL in 12 s 920 ms`. The HAP was reinstalled into the correct XDYOU bundle and launched as PID `28237`; the loaded timetable capture exposes 145 visible dot nodes. Center-pixel decoding measured 53 completed occupied dots at `RGB(142,167,198)` and 92 vacant dots at `RGB(209,218,231)`, proving the deep/shallow states are rendered; a bounded 5-second target log contains 0 app FATAL. Evidence: `evidence/classtable-week-dot-colors-final-v5-2026-08-14/` and its `pixel-validation.md`.
## 2026-08-14 — Class-table lower and evening periods

- Skills: `harmonyos-cangjie-dev` and `harmonyos-build-run-diagnose`; `cangjie-essentials.md` and the existing project configuration were read before editing.
- Source/reference: `UI真实数据展示截图/课程表下半部.png`, `source_2.0/lib/model/time_list.dart`, `class_table_view/class_table_view.dart`, and `class_table_view/class_organized_data.dart`.
- Adopted: the source's 61-block vertical model: periods 1–4, a three-block noon break, periods 5–8, a three-block supper break, then periods 9–11. A period remains 64vp and each break is 38.4vp; course top and bottom positions now include both breaks. Evening times are `19:00–19:45`, `19:55–20:35`, and `20:40–21:25`.
- RAG: no unfamiliar HarmonyOS API was introduced; the change uses the page's already verified ArkUI `Column`, `Stack`, `Scroll`, sizing, and position APIs, so source inspection was sufficient.
- Validation: `build_recovery.py --retry` reported `BUILD SUCCESSFUL in 12 s 301 ms`; the HAP installed into `io.github.benderblog.traintime_pda.harmonyos` and launched as PID `13288`. The genuine timetable was scrolled to its lower section; noon break, supper break, and periods 9, 10, and 11 passed 5/5 assertions. The bounded target log contains 0 app FATAL. Evidence: `evidence/classtable-evening-periods-2026-08-14/`.

## 2026-08-14 — Class-table week/card synchronization follow-up

- Skills: `harmonyos-cangjie-dev` and `harmonyos-build-run-diagnose`; `cangjie-essentials.md`, the Flutter `WeekChoiceView`/`ClassTableWidgetState` chain, and the previously verified parameterized-Builder state-retention lesson were read before editing.
- Diagnosis: Flutter's zero-based week indexing and 5×5 slot ranges match the Cangjie one-based model after conversion. The unsafe difference was the Cangjie page's parameterized `dayColumn(day)` Builder calling a derived `dayCourses(day)` list while week-dot colors were separately materialized. ArkUI could retain/reuse old `CourseSlot` nodes after a week switch, making weeks such as 7 and 9 display cards from a different reactive snapshot than their dots.
- Implementation: week selection now rebuilds both the complete dot-preview list and seven explicit per-day `@State ArrayList<CourseSlot>` values from the same `courses` snapshot. Seven non-parameterized day builders iterate those state lists directly, and each card key/id contains week, day, course identity, period range, and index so a prior week's nodes cannot survive the switch.
- Validation: the final build reported `BUILD SUCCESSFUL in 12 s 178 ms`; after the user-requested uninstall, the new HAP installed and launched as PID `31689`, with 0 app FATAL in a bounded log. The uninstall cleared the genuine login/session, so live week-7/week-9 paired card-dot capture remains `IN_PROGRESS` until login is restored. Evidence: `evidence/classtable-week-card-sync-2026-08-14/`.

# 2026-08-14 校园卡真实余额与流水修复

- Skills: `harmonyos-cangjie-dev`, `cangjie-harmonyos-knowledge`, `cangjie-core-reference`, `harmonyos-build-run-diagnose`, `harmonyos-evolution`.
- Commands: `cjdocs.py doctor`; `cjdocs.py query "NetworkKit HttpRequestOptions POST cookie redirects responseCode" --top-k 8`.
- Refs: `docs/API/NetworkKit/cj-apis-net-http.md#func-request-string-httprequestoptions-asynccallback`, `#class-httprequestoptions`, `#var-extradata`.
- Adopted: cookies remain explicit request headers; GET requests omit empty `extraData`; campus-card overview and transactions refresh OpenID once on a stale/invalid response; homepage and transaction page first use the persisted IDS session directly, with hidden Web authentication only as a single fallback. Flutter reference: `source_2.0/lib/repository/ids_session/school_card_session.dart`.
- Scope: electricity/water behavior is unchanged because it requires the campus network.
- Verification: final incremental HAP build reported `BUILD SUCCESSFUL in 1 min 24 s 475 ms`; replacement install and foreground launch succeeded on `127.0.0.1:5557` as PID `16065`. Live populated balance/rows remain `BLOCKED_EXTERNAL` because the persisted account reported `Ehall 会话已过期，请退出后重新登录。`; no mock campus-card data was added.
- Follow-up diagnosis: supplied runtime screenshots showed `Campus-card service returned HTTP 404` followed by repeated loading. Root cause was the generic Web authenticator accepting its four-cycle cookie fallback without an OpenID; each failed transaction then remounted the authenticator. Campus-card callers now require a non-empty OpenID and the detail page permits only one Web fallback cycle.
- Follow-up verification: `BUILD SUCCESSFUL in 2 min 18 s 694 ms`; replacement install and foreground launch succeeded as PID `23718`. The simulator's persisted IDS/Ehall session was expired at runtime, so populated values remain externally gated until the account signs in again; the new OpenID gate prevents the previous false-ready/404 refresh loop.
- Final correction: the live campus-card entry currently traverses `v8scan -> xxcapp -> IDS -> xxcapp -> v8scan`; direct HTTP SSO remained the source of the visible 404. Campus-card page/home now use the authenticated hidden Web path first and call the real APIs only after a non-empty OpenID. The range-calendar month/year labels now read `@State` inside their Builder, matching the working empty-classroom calendar update pattern.
- Verification: `BUILD SUCCESSFUL in 1 min 22 s 516 ms`; installed/launched as PID `6480`. Calendar capture changed the header from `Aug 2026` to `Sep 2026` together with the grid. The current emulator Ehall initialization returned HTTP 500, so a fresh valid login is still required for populated campus-card rows; the page no longer starts with the direct HTTP SSO path that produced the supplied 404.
- Android reference capture: `adb logcat --pid` showed the successful source flow as `GET openXDOAuth2Page (302) -> GET xxcapp OAuth (302 with code) -> GET openHomePage?code (200) -> POST queryCardSelfTradeList (200)`. The source does not GET `openSelfTrade` before the POST and sends only its browser User-Agent, content type and cookie. Harmony now matches that sequence after Web SSO; the extra transaction-page GET and AJAX-only headers were removed.
- Final implementation follows the same direct SSO chain first and invokes the JSON POST without the former `openSelfTrade` request that returned 404. Hidden Web authorization is mounted only if direct OpenID acquisition fails, so neither homepage nor detail page remains in a refresh loop. Final HAP: `BUILD SUCCESSFUL in 1 min 45 s 120 ms`; replacement install succeeded. Runtime population could not be rechecked because the emulator's retained Ehall/IDS session had expired by the final launch; no mock balance or records were introduced.
## 2026-08-14 — model/repository module-folder refactor

- Skills read completely: `harmonyos-cangjie-dev`, `harmonyos-project-bootstrap`, `cangjie-harmonyos-knowledge`, `cangjie-core-reference`, `harmonyos-build-run-diagnose`, `harmonyos-evolution`, and `.agents/skills/cangjie-essentials.md`. The existing project, SDK `6.1.0(23)`, and both OHOS ABI targets were preserved; no project bootstrap or ArkTS interop was performed.
- Project inspection: read `build-profile.json5`, `entry/cjpm.toml`, `entry/src/main/module.json5`, the complete `entry/src/main/cangjie/` file inventory, project `Evolution.md`, and packaged evolution memory before editing.
- Required RAG commands: the configured `python .agents/skills/cangjie-harmonyos-knowledge/rag/cjdocs.py --config ./cjdocs.toml doctor` initially returned `OperationalError: unable to open database file` because the packaged index directory is read-only in the sandbox. A temporary writable index copy was used with `--index-dir .work/cjdocs`; `doctor` then reported 644 documents and 12,557 sections, and `query "ArkUI Button TextInput state onClick" --top-k 8` completed successfully.
- RAG refs read completely: `docs/API/arkui-cj/cj-text-input-textinput.md#示例代码` and `docs/Guide/arkui-cj/cj-common-components-button.md#场景示例`. These satisfied the mandatory project retrieval gate; no new ArkUI API was needed by the directory-only refactor.
- Cangjie language reference: read `references/cangjie-lang-features/REFERENCE.md` and `references/cangjie-lang-features/package/README.md`. Adopted conclusions: package declarations must match source-relative directories, imports cannot form cycles or import the current package, and public re-exports can preserve the existing root `model.*` and `repository.*` consumer boundary.
- Implementation: grouped models under `miscellaneous_session`, `xidian_ids`, `pda_service`, and `xidian_sport`; grouped repositories under `miscellaneous_session`, `xidian_ids`, `experiment_score`, and `pda_service`; split `AirconEnergyInfo` from IDS `EnergyInfo`/`MeterReading`; added root `package.cj` re-export facades; removed child-to-parent repository imports that would create dependency cycles; updated migration, acceptance, and evidence paths.
- Build verification: the untouched baseline and final refactored tree both passed `build_recovery.py --retry`. Final `SyncCangjieResource` reported `BUILD SUCCESSFUL in 851 ms`; final `assembleHap` reported `BUILD SUCCESSFUL in 27 s 93 ms`; output HAP: `entry/build/default/outputs/default/entry-default-unsigned.hap`.

## 2026-08-14 — business-feature package correction

- Skills: re-read `harmonyos-cangjie-dev`, `cangjie-core-reference`, `harmonyos-build-run-diagnose`, `cangjie-essentials.md`, and the complete package mechanism reference before changing the previously provider-grouped layout.
- Adopted structure: `model/` and `repository/` now use business features such as `auth`, `class_attendance`, `classtable`, `custom_class`, `dorm_water`, `empty_classroom`, `energy`, `experiment`, `library`, `school_card`, `schoolnet`, `score`, and `sport`. Technical-source folders `xidian_ids`, `miscellaneous_session`, `experiment_score`, `pda_service`, and `xidian_sport` were removed.
- Dependency rule: leaf repository packages import only the required feature packages (`auth`, `academic`, or model facade); the root `model/package.cj` and `repository/package.cj` remain one-way public re-export facades for aggregate consumers. `IDSSession.loginUrl` and `browserUserAgent` were made public because energy and library are now separate packages and legitimately consume those shared authentication constants.
- Build diagnosis: the first build exposed the expected cross-package visibility errors for `IDSSession.loginUrl` and `browserUserAgent`; `build_analyzer.py` identified those as the first stable error block. After the minimal visibility fix, `SyncCangjieResource` reported `BUILD SUCCESSFUL in 634 ms` and `assembleHap` reported `BUILD SUCCESSFUL in 25 s 422 ms`.
- Structural verification: every `.cj` package declaration matches its directory; no old technical-package import remains; `find entry/src/main/cangjie/model entry/src/main/cangjie/repository -type d -empty` returns no directories; `git diff --check` passes. HAP: `entry/build/default/outputs/default/entry-default-unsigned.hap`.

## 2026-08-14 — 水电真实数据与首页读表

- Skills: `harmonyos-cangjie-dev`、`harmonyos-build-run-diagnose`；同时完整读取了工作流要求的基础说明与 `cangjie-essentials.md`。
- Source chain: 对照 `source_2.0/lib/repository/ids_session/energy_session.dart` 和 Android 模拟器，采用 `xxcapp OAuth code -> GetSignature -> OauthGetUserInfo -> H5UserIDLogIn -> H5QueryMeterList -> GetMetRead`，签名请求继续使用源应用相同的 AES-CBC 数据、显式 Cookie 与请求头。
- RAG: `cjdocs.py doctor` 与 query `ArkUI Button TextInput state onClick` 因本机索引 `OperationalError: unable to open database file` 未返回命中；随后直接阅读 `docs/API/NetworkKit/cj-apis-net-http.md` 的 `request`、`HttpRequestOptions`、`extraData`、headers 与 response 小节。采用结论：GET 不附带空 body，表单 POST 通过 `extraData` 并显式设置 Content-Type/Cookie。
- Implementation: Web 授权必须取得非空 `code` 才允许回调；同时请求电表月记录和水表年记录；详情成功结果写入静态控制器缓存，首页直接读取余额和最后读表日期。
- Verification: 独立 Hvigor 缓存下 `BUILD SUCCESSFUL in 2 min 49 s 372 ms`，HAP 替换安装并启动。真实运行得到电量 `-62.70 kWh`、最后读表 `2026-08-14`；水表表格存在 4 条真实记录（包括 `2026-07-09 / 17.0 / 220.0 / 203.0`）；返回首页显示 `余额 -62.70 度` 和 `最后一次读表：2026-08-14`。目标进程日志 0 FATAL。
- Runtime evidence: `acceptance/runtime/energy-detail-real-chain.json`、`energy-bottom.json`、`energy-home-after-back.json`，日志目录 `evidence/energy-real-runtime-2026-08-14/`。
- UI follow-up: 水表时间压缩为日期、三项读数统一为 1 位小数，避免源数据的 6 位小数导致窄列换行。补齐 `std.convert` 后最终复验 `BUILD SUCCESSFUL in 1 min 29 s 794 ms`，HAP 替换安装并启动成功。
