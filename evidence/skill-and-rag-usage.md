# Skill and RAG usage

| Skill / source | Command or reference | Adopted conclusion |
| --- | --- | --- |
| harmonyos-cangjie-dev | Read complete SKILL.md | Used the required Cangjie incremental migration workflow. |
| harmonyos-project-bootstrap | Read complete SKILL.md | Preserved the existing DevEco project and both ABI targets. |
| cangjie-harmonyos-knowledge | `python .agents/skills/cangjie-harmonyos-knowledge/rag/cjdocs.py --config ./cjdocs.toml doctor` | Failed with `OperationalError: unable to open database file`; raw packaged docs were used as fallback. |
| cangjie-harmonyos-knowledge | `query "ArkUI Button TextInput state onClick"` | Same database failure; no query result was treated as evidence. |
| Packaged ArkUI touch docs | `rag/docs/API/arkui-cj/cj-common-types.md`, `cj-universal-event-touch.md`, `cj-common-events-touch-screen-event.md` | Used TouchEvent coordinates, changedTouches, event type, and nanosecond timestamps to serialize manual slider tracks. |
| Packaged NetworkKit docs | `rag/docs/API/NetworkKit/cj-apis-net-http.md`, `Guide/network/cj-http-request.md` | Used HttpRequestOptions, response code, cookies, and response headers for IDS requests. |
| cangjie-core-reference | Read complete SKILL.md and collection references | Used ArrayList and HashMap with an explicit import to avoid ArkUI Stack namespace ambiguity. |
| harmonyos-build-run-diagnose | `build_analyzer.py`, `ui_capture.py`, `hilog_capture.py` | Built, installed, foreground-checked, captured screenshot/component tree, and bounded target hilog. |
| harmonyos-evolution | Read complete SKILL.md | Recorded only verified Phase 1 conclusions in migration and acceptance records. |

No student account, password, cookie, token, API key, or captcha payload is retained in evidence.
| Phase 2 RAG fallback | doctor and query HTTP JSON date time List Scroll state main thread both returned OperationalError: unable to open database file; read packaged NetworkKit, ArkUI list/refresh, state and stdx JSON docs | Used explicit HTTP timeouts/string response type, main-thread launch, Scroll/List rendering and stdx JSON parsing. |
| harmonyos-build-run-diagnose | Phase 2 ssembleApp, ui_capture.py, hilog_capture.py | Timetable, score and exam implementation builds and installs; real academic data remains externally blocked. |
| harmonyos-evolution | Read SKILL.md and verified evidence | Recorded only build/device and external-gate conclusions in phase handoff. |
| cangjie-core-reference | Packaged `std.time` DateTime and comparison reference | Parsed examination start times and implemented deterministic chronological grouping. |
| harmonyos-build-run-diagnose | Hvigor build, x86_64 install/cold launch, `ui_capture.py`, `hilog_capture.py` | Verified the exam slice compiles, packages, launches and produces target-process evidence. |
| Packaged ArkWeb guide | `Guide/web/cj-web-page-loading-with-web-components.md` | Added INTERNET-backed Web component for the seven toolbox destinations. |
| Packaged ArkWeb API | `cj-apis-webview.md` sections `loadUrl`, `canGoBack`, `goBack` | Added guarded URL navigation and Web history return. |
| Cangjie SDK declarations and compile probes | NotificationKit, CalendarKit and FormKit searches plus three Hvigor probes | Marked notification, calendar and desktop card `BLOCKED_EXTERNAL` under SDK 6.1. |
| cangjie-arkts-interop | Read complete SKILL.md plus `cangjie-call-arkts.md`, `hybrid-projects.md`, interop-lib README and packaged `cangjie-load-arkts.md` | Kept the main Ability/UI in Cangjie and isolated unavailable notification/calendar calls behind one JSRuntime bridge. |
| Packaged AccessToken docs | Read complete `cj-apis-ability_access_ctrl.md`, `cj-request-user-authorization.md`, and `cj-permissions-for-all-user.md` | Declared and dynamically requested both calendar user-grant permissions before calling Calendar Kit. |
| Cangjie SDK ArkTS declarations | Read `@ohos.calendarManager.d.ts`, `@ohos.app.form.FormExtensionAbility.d.ts`, `@ohos.app.form.formProvider.d.ts`, and UIAbility `toJSValue` declaration | Implemented the actual calendar callback chain and retained desktop card as an independent ArkTS extension boundary. |
| cangjie-harmonyos-knowledge | Query `Cangjie abilityAccessCtrl requestPermissionsFromUser READ_CALENDAR WRITE_CALENDAR UIAbilityContext` | RAG database again returned `OperationalError`; required packaged documents were read directly instead. |
| harmonyos-build-run-diagnose | Repeated Hvigor builds, x86_64 install/cold launch, UI tree and bounded hilog capture | IDS fallback, notification/calendar bridge, attendance and public library search produced successful HAPs; latest schoolnet addition awaits build because platform escalation quota was exhausted. |
| Static integrity follow-up | Strict `ConvertFrom-Json` plus targeted UTF-8/mojibake and placeholder scans | Repaired a corrupted calendar permission resource and schoolnet strings before any later build; existing HAP explicitly predates schoolnet. |
| cangjie-harmonyos-knowledge | Query `Cangjie ImageSource createPixelMap readPixelsToBuffer image decode base64 PixelMap`; RAG again returned `OperationalError` | Read packaged image decoding and PixelMap operation guides completely and used `Rgba8888`, `getImageInfo`, `readPixelsToBuffer`, and explicit release. |
| cangjie-core-reference | Complete random, math, and basic data-type references | Ported Flutter random trajectory generation, sigmoid/round operations, numeric conversions, and timed retries. |
| harmonyos-build-run-diagnose | Repeated Hvigor repair loop plus synthetic UI scenarios and bounded hilog | Latest HAP builds, installs and cold-launches; automatic verification remains server-rejected and is not marked PASS. |
| Packaged ArkWeb cookie and script APIs | Read complete `cj-apis-webview.md` sections `WebCookieManager` and `runJavaScript`, plus `cj-web-web.md` Web creation and `onPageEnd` | NetworkKit did not expose IDS Set-Cookie headers; retained the custom Cangjie login UI while submitting the official IDS password form inside a hidden ArkWeb session. Credentials cross only in volatile memory and are cleared after submission; no secret is logged or persisted. |

| cangjie-harmonyos-knowledge | Query `Cangjie NetworkKit HttpData ArrayData bytes response Preferences encrypted token secure storage`; RAG returned `OperationalError: unable to open database file`; read packaged `NetworkKit/cj-apis-net-http.md` and `ArkData/cj-apis-preferences.md` | Used ArrayBuffer captcha responses, bounded HTTP timeouts, Preferences flush/delete, and encrypted-at-rest drinking-water tokens. |
| harmonyos-build-run-diagnose | Dorm-water NetworkKit/Preferences slice: Hvigor dual-ABI build, x86_64 install/launch, `ui_capture.py`, and bounded `hilog_capture.py` | `BUILD SUCCESSFUL in 3 min 1 s 609 ms`; emulator install and PID confirmation passed. Runtime feature interaction remains gated by unavailable IDS home navigation. |
| Cangjie main-thread runtime diagnosis | Read faultlogger `cjerror-io.github.benderblog.traintime_pda.harmonyos-20020065-20260803144535026.log` and moved ArkWeb callback state mutations into `launch {}` | Removed the reproducible login-button process exit caused by updating `@State` outside the UI thread; post-fix cold launch and component-tree capture are clean. |
| harmonyos-build-run-diagnose | Replayed the repository synthetic Login-click scenario after the main-thread fix | The UI reached `Working...`, PID 8697 remained alive, and the target faultlogger gained no post-fix crash; this verifies crash removal only, not genuine authentication. |
| cangjie-core-reference | Read complete `references/cangjie-std/sync/README.md` Timer section | Retained and cancelled `Timer.repeat` across page lifecycle; drinking-water status is polled every 60 seconds and stops after three consecutive idle responses. |
| harmonyos-build-run-diagnose | Rebuilt and reinstalled the timer-complete dorm-water HAP | `BUILD SUCCESSFUL in 1 min 48 s 780 ms`; emulator process PID 11752 and no new target faultlogger entry. |
| harmonyos-cangjie-dev / harmonyos-project-bootstrap | Read complete `SKILL.md` files and inspected `build-profile.json5`, `entry/cjpm.toml`, `module.json5`, and Cangjie sources for the XDYOU launch task | Preserved the existing project, compatible SDK `6.1.0(23)`, and both OHOS ABI targets; no re-scaffolding was performed. |
| cangjie-harmonyos-knowledge | Required `doctor` initially hit the read-only skill directory; built an offline writable index with `--index-dir evidence/cjdocs-index --ai off`, then ran `doctor` and `query "ArkUI Button TextInput state onClick" --top-k 8` | Index health passed with 644 documents and 12,557 sections; vectors=0 is valid for offline lexical retrieval. |
| cangjie-harmonyos-knowledge | Read `docs/API/arkui-cj/cj-text-input-textinput.md#示例代码` and `docs/Guide/arkui-cj/cj-common-components-button.md#场景示例` | Confirmed the existing Cangjie login page's TextInput state/onChange and Button onClick forms before rebuilding. |
| harmonyos-build-run-diagnose | Baseline `build_recovery.py --retry`, first-error analysis, repaired stale stdx paths, rebuilt, then `ui_capture.py` and `hilog_capture.py` against `127.0.0.1:5555` | Final HAP built successfully, installed successfully, foreground PID 30985 was confirmed, UI tree contains `XDYOU`, and bounded target app log has 0 FATAL/ERROR lines. |
| cangjie-harmonyos-knowledge | Reran doctor and ArkUI Preferences queries; local index returned `OperationalError: unable to open database file`; read complete packaged Preferences API and persistence guide | Used Preferences get/put/flush and a dedicated local store without treating RAG snippets as API proof. |
| cangjie-core-reference | Read complete stdx JSON and std.time references plus std.sort comparator examples | Implemented JSON codec, exact-date validation/date arithmetic, and stable occurrence sorting with the global `sort` API. |
| harmonyos-build-run-diagnose | Hvigor dual-ABI repair loop, x86_64 install/launch, three stable-ID UI scenarios, component trees, screenshots, and bounded hilog | CRUD, restart persistence, and synthetic-data cleanup each passed 2/2 assertions; timetable live-data assertion remains open. |
| harmonyos-cangjie-dev | Re-read complete workflow and `cangjie-essentials.md`; reused existing ArkUI State Button conditional rendering and stable IDs | Added the smallest in-memory preview boundary without changing IDS persistence or network code. |
| harmonyos-build-run-diagnose | Dual-ABI Hvigor build plus x86_64 install launch stable-ID entry exit component-tree screenshot and bounded hilog checks | Mock home preview passed 4/4 assertions; exit back to real login passed 3/3; no target fatal line. |
| harmonyos-cangjie-dev / harmonyos-project-bootstrap / harmonyos-evolution | Re-read the complete workflow files, `cangjie-essentials.md`, project configuration, `Evolution.md`, and packaged memory for the five-bottom-tab alignment slice | Preserved the existing DevEco project, compatible SDK `6.1.0(23)`, both OHOS ABI targets, and implemented the UI as a separate incremental Cangjie file. |
| Flutter source UI references | Read `source/lib/page/homepage/home.dart`, `homepage.dart`, Ruisi `home_page.dart`, `toolbox_page.dart`, `pig_page.dart`, `setting.dart`, theme and card sources | Kept the original order and labels: Campus, Ruisi, Toolbox, Pig gallery, Settings; reused the Material 3 blue palette, header/list/card hierarchy, and honest loading/blocked states. |
| cangjie-harmonyos-knowledge | `doctor`; queries `ArkUI Tabs TabContent bottom navigation bar selected index onChange` and `ArkUI Grid GridItem List Divider Toggle Image Cangjie`; read Tabs init/onChange and the returned Grid/Toggle refs from `evidence/cjdocs-index` | Confirmed the offline index is healthy (644 documents, 12,557 sections) and used standard state-driven content switching with explicit stable IDs; no remote synthesis was used. |
| harmonyos-build-run-diagnose | Two successful `build_recovery.py --retry` builds; final install and ten-step `five-bottom-tabs.json` scenario on `127.0.0.1:5555`; final bounded hilog | Final HAP built and installed; all five tabs were clicked and captured, 3/3 assertions passed, PID 30127 was confirmed, and target app lines contain 0 FATAL/ERROR/WARN. |
| harmonyos-cangjie-dev / Flutter source UI reference | Compared `UI截图/0.png` with `source/lib/page/homepage/home.dart`, `homepage.dart`, `homepage_widget_registry.dart`, the Traditional Chinese locale and existing `aligned_home_page.cj` | Rebuilt the campus page and five-item bottom bar around the reference hierarchy, Traditional Chinese labels, beta/schedule/status cards, four-column tools and selected blue navigation capsule while preserving existing navigation callbacks. |
| cangjie-harmonyos-knowledge | Writable-index `doctor`; query `ArkUI Row Column layoutWeight borderRadius backgroundColor bottom navigation selected`; read `layoutWeight`, four-corner `borderRadius`, and `Image.fillColor` refs | Index passed with 644 documents and 12,557 sections. SVG media resources use `Image(@r(...)).fillColor(...)`; weighted page content continues to reserve the fixed bottom bar. |
| harmonyos-build-run-diagnose | Final `build_recovery.py --retry`, install/launch and `home-nav-alignment.json` capture on `127.0.0.1:5555`, then bounded hilog | Final HAP reports `BUILD SUCCESSFUL`, installation and foreground PID 4243 passed, reference-key assertions passed 5/5, and the target remained alive with 0 FATAL; the single UIAbility continuation diagnostic is non-fatal emulator startup noise. |
| harmonyos-cangjie-dev / cangjie-core-reference | Read complete workflow essentials and std time/formatting references for the Phase C campus-card source chain | Preserved controller/model/repository/page structure with main-thread state updates and no fabricated authenticated data. |
| cangjie-harmonyos-knowledge | `doctor` and required ArkUI/NetworkKit queries failed with `OperationalError: unable to open database file`; read raw packaged NetworkKit HTTP and ArkUI TextInput/Image material as fallback | Used explicit response type timeouts status checks redirect Location handling and existing compiled Image data-URI pattern. |
| harmonyos-build-run-diagnose | Repaired stable compiler errors then completed dual-ABI Hvigor build x86_64 install launch 4/4 stable-ID scenario and bounded hilog | Campus-card offline boundary passes; real IDS SSO data remains BLOCKED_EXTERNAL and payment QR was excluded from evidence. |
| harmonyos-cangjie-dev / Flutter and screenshot references | Read `UI截图/6.png`, `source/lib/page/pig/pig_page.dart`, `source/lib/repository/pighub_session.dart`, the Traditional Chinese locale, project configuration, `cangjie-essentials.md`, `Evolution.md`, and packaged memory | Replaced the static Pig placeholder with a source-aligned Traditional Chinese page, real PigHub data chain, loading/error/image states, outlined actions, and the selected Pig navigation capsule. |
| cangjie-harmonyos-knowledge | Writable-index `doctor`; queries for NetworkKit HTTP, network Image events and `@Prop`; read `cj-apis-net-http.md` request/response-type sections, `cj-image-video-image.md` onComplete/onError, `cj-common-types.md` ImageFit, and the complete `cj-macro-prop.md` guide | Used explicit string HTTP responses and timeouts, Image completion/error callbacks, and direct parent `@State` to child `@Prop` synchronization for bottom-navigation selection. |
| cangjie-core-reference | Read complete std random and stdx JSON references | Parsed the real PigHub JSON list, cached it, selected a random item, and avoided immediately repeating the prior PigHub ID. |
| harmonyos-build-run-diagnose / harmonyos-evolution | Final Hvigor build, emulator install, combined before/after interaction scenario, visual inspection, screenshot hashes and bounded hilog | `BUILD SUCCESSFUL in 6 s 985 ms`; 4/4 UI assertions passed; `Change A Pig` changed title and image in one session; target app log contains 0 FATAL/ERROR/WARN. The verified direct-state `@Prop` lesson was recorded. |
| harmonyos-cangjie-dev / Flutter and screenshot references | Read complete workflow and essentials; compared `UI截图/2.png` with `toolbox_page.dart`, `webview_list_tile.dart`, `zh_TW.yaml`, and the existing Cangjie page | Matched the seven-row borderless list, Traditional Chinese copy, spacing, monochrome icons and selected Toolbox navigation state while preserving all source URLs. |
| cangjie-harmonyos-knowledge | Writable-index `doctor`; query `ArkUI Cangjie Image SVG fillColor Row spacing padding list item`; read `cj-image-video-image.md#func-fillcolor-resourcecolor` and `cj-universal-attribute-size.md#func-padding-length-length-length-length` | Index passed with 644 documents and 12,557 sections; used SVG-only `fillColor` tinting and named four-edge padding with explicit vp units. |
| harmonyos-build-run-diagnose | Two successful builds, final emulator install/launch, `toolbox-alignment.json` capture and bounded hilog | Final build passed in 7 s 148 ms; PID 30731 confirmed; all 8 UI assertions passed and target app lines contain 0 FATAL/ERROR/WARN. |
| harmonyos-cangjie-dev / cangjie-core-reference | Read the Flutter sport repository/model/page chain and packaged stdx crypto, encoding and std IO references | Implemented RSA-PKCS1, MD5/hex, Base64/ByteBuffer, real NetworkKit forms, HUKS ciphertext storage and source-structured model/repository/controller/page files. |
| cangjie-harmonyos-knowledge | Required `doctor` and RSA/MD5/form query returned `OperationalError: unable to open database file`; read raw packaged CryptoArchitectureKit MD5, NetworkKit `HttpRequestOptions`, ArkUI TextInput and stdx crypto references | Used the packaged raw docs as the declared fallback; no failed RAG snippet was treated as an API signature. |
| harmonyos-build-run-diagnose | User-side DevEco Studio build and HarmonyOS emulator launch report for the sports working tree | Records only the user-confirmed build/launch boundary; no local PID, screenshot, hilog or genuine sports-data PASS is claimed. |
| cangjie-harmonyos-knowledge | Query `ImageKit createImageSource createPixelMap readPixelsToBuffer NetworkKit HttpData ArrayData` returned `OperationalError`; read complete packaged `cj-image-decoding.md`, `cj-image-pixelmap-operation.md` and ImageKit API sections for byte image sources and pixel buffers | Used RGBA8888 decoding, exact pixel-buffer sizing, explicit PixelMap/ImageSource release and NetworkKit ArrayBuffer image responses. |
| cangjie-core-reference | Read packaged std time, regex, Option and ArrayList sorting references | Used `DateTime.parse`, supported comparisons, grouped regex matches, safe Option rendering and named sort comparator syntax during compiler repair. |
| harmonyos-build-run-diagnose | Rebuilt the interrupted energy slice, repaired first stable Cangjie errors with build analyzer guidance, then ran x86_64 `ui_capture.py` and bounded `hilog_capture.py` | Final HAP build succeeded in 47 s 681 ms; energy boundary scenario passed 3/3 on `127.0.0.1:5557`; target app had 0 FATAL. |
| cangjie-core-reference | Read packaged `std.time` DateTime/Duration reference and reused existing `std.regex`, `std.convert` and stdx JSON patterns | Parsed server timestamps without unverified DateTime constructors, validated IMEI input before requests, and kept response parsing/error paths explicit. |
| harmonyos-cangjie-dev / harmonyos-build-run-diagnose | Re-read both complete workflow files; ran `build_recovery.py --retry`, then `ui_capture.py` and an 8-second `hilog_capture.py` against `127.0.0.1:5555` | Fresh HAP built with `BUILD SUCCESSFUL`, installed successfully, and launched as PID 11260. The foreground tree and screenshot show the XDYOU login page; target-related logs contain 0 FATAL. The sole target-name ERROR is system SCB feature-map noise, not an application-process failure. Evidence: `evidence/redeploy-2026-08-04/`. |
| harmonyos-cangjie-dev / project-bootstrap / evolution | Read all required workflow files plus `cangjie-essentials.md`; inspected project configs, Cangjie sources, `Evolution.md`, packaged memory and the existing settings migration evidence | Preserved the existing DevEco project, compatible SDK `6.1.0(23)`, both OHOS ABI targets and incremental Cangjie-only implementation; recorded only verified visual/runtime conclusions. |
| cangjie-harmonyos-knowledge | Required configured `doctor`/`ArkUI Button TextInput state onClick` query returned `OperationalError`; reran `doctor` and queries with `--index-dir evidence/cjdocs-index --ai off`, then read `cj-button-picker-toggle.md#示例1-设置开关的样式`, `#func-onchange-bool-unit`, `cj-scroll-swipe-scroll.md#scroll`, and four-corner `borderRadius` | Writable offline index passed with 644 documents and 12,557 sections; implemented typed Toggle callbacks, state-backed switches, clipped scrolling and named corner radii from full refs rather than snippets. |
| harmonyos-build-run-diagnose | Unmodified baseline build; first-stable-error analysis; final `build_recovery.py --retry`; Traintime reference HDC captures; final install and `settings-alignment.json`; 8-second target hilog | Final HAP built in 7 s 853 ms, installed and launched as PID 15098; settings scenario passed 9/9 steps and 4/4 assertions; target app lines contain 0 FATAL/ERROR/WARN. Evidence: `evidence/settings-page-alignment-2026-08-04.md`. |
| harmonyos-cangjie-dev / harmonyos-build-run-diagnose | Converted the aligned settings page to the checked-in `zh_TW` copy, rebuilt, reinstalled, replayed the full scroll scenario and captured bounded hilog | Final follow-up build passed in 7 s 842 ms; 9/9 steps and 4/4 assertions passed; target app lines contain 0 FATAL/ERROR/WARN. Evidence: `acceptance/runtime/2026-08-04-settings-traditional-final/`. |
| harmonyos-cangjie-dev / harmonyos-project-bootstrap / harmonyos-evolution | Read complete SKILL files, `cangjie-essentials.md`, project configuration and `Evolution.md` before the Sysj slice | Preserved the existing pure-Cangjie project; this user-scoped slice compiles and runs only the x86_64 emulator target. |
| cangjie-harmonyos-knowledge | `cjdocs.py --config ./cjdocs.toml doctor`; query `NetworkKit HttpRequestOptions redirects response headers Cookie Location`; read `docs/API/NetworkKit/cj-apis-net-http.md#func-request-string-httprequestoptions-asynccallback` and `#class-httprequestoptions` | Confirmed explicit Cookie headers, `HttpRequestOptions` timeouts/string response type and NetworkKit request limits for the Sysj SSO chain. |
| cangjie-core-reference | Read std time, regex, collection and stdx encoding reference indexes | Used `DateTime.parse`, Regex table/cell extraction, `ArrayList` merging/sorting and explicit form/query percent encoding without ArkTS. |
| harmonyos-build-run-diagnose | First build analyzed with `build_recovery.py --retry`; SDK target `6.1.1(24)` missing, then x86_64 rebuild, `ui_capture.py`, and bounded `hilog_capture.py` against `127.0.0.1:5557` | Root target SDK was aligned to installed compatible SDK `6.1.0(23)`; final `BUILD SUCCESSFUL in 1 min 30 s 590 ms`, HAP install/launch PID 16982 and experiment scenario 4/4 passed. Live Sysj data remains externally blocked. |
