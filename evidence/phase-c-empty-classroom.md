# Phase C empty-classroom slice

Implemented from the Flutter model, repository and both pages:

- teaching-building query `jxlcx.do`;
- date-to-week/weekday conversion `rqzhzcjc.do`;
- classroom occupancy query `cxjsqk.do`;
- building code, date, semester and classroom-name filters;
- complete 1-11 period occupancy matrix with stable component ids;
- loading, empty, successful and propagated network/parse error states.

The implementation uses the genuine Ehall cookie gate and stores no response bodies or secrets. Hvigor passed after the page was wired into the academic hub.

Live endpoint data, exact result counts and the paired Android screenshot remain `BLOCKED_EXTERNAL` until a genuine IDS/Ehall session is available.
